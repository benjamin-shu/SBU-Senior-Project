label CSE_uml_1:
    $ phase += 1
    if phase < 3:
        $ renpy.jump("week_phase_%d", phase)
    else:
        $ phase = 0
        jump week_end

label CSE_uml_2:
    $ phase += 1
    if phase < 3:
        $ renpy.jump("week_phase_%d", phase)
    else:
        $ phase = 0
        jump week_end

label CSE_srs_1:
    $ phase += 1
    if phase < 3:
        $ renpy.jump("week_phase_%d", phase)
    else:
        $ phase = 0
        jump week_end

label CSE_srs_2:
    $ phase += 1
    if phase < 3:
        $ renpy.jump("week_phase_%d", phase)
    else:
        $ phase = 0
        jump week_end

label CSE_phase1_1:
    $ phase += 1
    if phase < 3:
        $ renpy.jump("week_phase_%d", phase)
    else:
        $ phase = 0
        jump week_end

label CSE_phase1_2:
    $ phase += 1
    if phase < 3:
        $ renpy.jump("week_phase_%d", phase)
    else:
        $ phase = 0
        jump week_end

label CSE_phase1_3:
    $ phase += 1
    if phase < 3:
        $ renpy.jump("week_phase_%d", phase)
    else:
        $ phase = 0
        jump week_end

label CSE_phase2_1:
    $ phase += 1
    if phase < 3:
        $ renpy.jump("week_phase_%d", phase)
    else:
        $ phase = 0
        jump week_end

label CSE_phase2_2:
    $ phase += 1
    if phase < 3:
        $ renpy.jump("week_phase_%d", phase)
    else:
        $ phase = 0
        jump week_end

label CSE_phase2_3:
    $ phase += 1
    if phase < 3:
        $ renpy.jump("week_phase_%d", phase)
    else:
        $ phase = 0
        jump week_end

label CSE_phase3_1:
    $ phase += 1
    if phase < 3:
        $ renpy.jump("week_phase_%d", phase)
    else:
        $ phase = 0
        jump week_end

label CSE_phase3_2:
    $ phase += 1
    if phase < 3:
        $ renpy.jump("week_phase_%d", phase)
    else:
        $ phase = 0
        jump week_end

label CSE_phase3_3:
    $ phase += 1
    if phase < 3:
        $ renpy.jump("week_phase_%d", phase)
    else:
        $ phase = 0
        jump week_end

label CSE_phase3_4:
    $ phase += 1
    if phase < 3:
        $ renpy.jump("week_phase_%d", phase)
    else:
        $ phase = 0
        jump week_end
